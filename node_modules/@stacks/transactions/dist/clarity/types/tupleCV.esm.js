import { ClarityType } from '../clarityValue.esm.js';
import { isClarityName } from '../../utils.esm.js';

function tupleCV(data) {
  for (var key in data) {
    if (!isClarityName(key)) {
      throw new Error("\"" + key + "\" is not a valid Clarity name");
    }
  }

  return {
    type: ClarityType.Tuple,
    data: data
  };
}

export { tupleCV };
//# sourceMappingURL=tupleCV.esm.js.map
